apiVersion: v1
kind: Service
metadata:
  name: questnutri-admin-panel-service
spec:
  selector:
    app: questnutri-admin-panel-deployment
  type: LoadBalancer
  ports:
    - port: 14041
      targetPort: 80
      protocol: TCP
      name: http