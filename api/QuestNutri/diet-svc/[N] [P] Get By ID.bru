meta {
  name: [N] [P] Get by ID
  type: http
  seq: 5
}

get {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/diet/:dietId
  body: none
  auth: bearer
}

params:path {
  dietId: {{createdDiet}}
}

auth:bearer {
  token: {{NUTRITIONIST_ACCESS_TOKEN}}
}

vars:pre-request {
  dietId: {{createdDiet}}
}

settings {
  encodeUrl: true
  timeout: 0
}
