meta {
  name: [P] Current
  type: http
  seq: 1
}

get {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/diet/current
  body: none
  auth: bearer
}

auth:bearer {
  token: {{PATIENT_ACCESS_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
