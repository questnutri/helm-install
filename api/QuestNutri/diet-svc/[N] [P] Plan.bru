meta {
  name: [N] [P] Plan
  type: http
  seq: 6
}

get {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/diet/diets/plan?length=24
  body: json
  auth: bearer
}

params:query {
  length: 24
}

auth:bearer {
  token: {{NUTRITIONIST_ACCESS_TOKEN}}
}

body:json {
  {
    "patientId": "{{PATIENT_ID}}",
    "nutritionistId": "{{NUTRITIONIST_ID}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
