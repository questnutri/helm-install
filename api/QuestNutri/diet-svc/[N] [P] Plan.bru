meta {
  name: [N] [P] Plan
  type: http
  seq: 6
}

get {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/diet/:dietId/plan?monthlyView=true&date=2025-11-01&includeRecords=true
  body: json
  auth: bearer
}

params:query {
  monthlyView: true
  date: 2025-11-01
  includeRecords: true
  ~length: 24
}

params:path {
  dietId: {{createdDiet}}
}

auth:bearer {
  token: {{NUTRITIONIST_ACCESS_TOKEN}}
}

body:json {
  {
    "patientId": "{{PATIENT_ID}}",
    "nutritionistId": "{{NUTRITIONIST_ID}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
