meta {
  name: [N] [P] Plan
  type: http
  seq: 7
}

get {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/diet/:dietId/plan?includeRecords=true
  body: json
  auth: bearer
}

params:query {
  includeRecords: true
  ~monthlyView: true
  ~date: 2025-11-01
  ~length: 24
}

params:path {
  dietId: {{createdDiet}}
}

auth:bearer {
  token: {{NUTRITIONIST_ACCESS_TOKEN}}
}

body:json {
  {
    "patientId": "{{PATIENT_ID}}",
    "nutritionistId": "{{NUTRITIONIST_ID}}"
  }
}

vars:pre-request {
  dietId: cc4f791f-d02b-4d04-aad2-ba307bd4bc2c
}

settings {
  encodeUrl: true
  timeout: 0
}
