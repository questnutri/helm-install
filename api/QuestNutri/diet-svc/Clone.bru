meta {
  name: Clone
  type: http
  seq: 7
}

post {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/diet/:dietId/clone
  body: none
  auth: bearer
}

params:path {
  dietId: {{createdDiet}}
}

auth:bearer {
  token: {{NUTRITIONIST_ACCESS_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
