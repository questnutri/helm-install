meta {
  name: Activate
  type: http
  seq: 5
}

post {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/diet/:dietId/activate
  body: none
  auth: bearer
}

params:path {
  dietId: {{createdDiet}}
}

auth:bearer {
  token: {{NUTRITIONIST_ACCESS_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
