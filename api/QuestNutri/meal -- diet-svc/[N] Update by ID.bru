meta {
  name: [N] Update by ID
  type: http
  seq: 6
}

patch {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/diet/meals/:mealId
  body: json
  auth: bearer
}

params:path {
  mealId: {{createdMeal}}
}

auth:bearer {
  token: {{NUTRITIONIST_ACCESS_TOKEN}}
}

body:json {
  {
    "name": "Test update",
    "hour": "09:00",
    "repeatConfiguration": {
      "type": "WEEKLY"
    }
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
