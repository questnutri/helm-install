meta {
  name: [N] Delete by ID
  type: http
  seq: 8
}

delete {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/patient/:patientId
  body: none
  auth: bearer
}

params:path {
  patientId: {{PATIENT_ID}}
}

auth:bearer {
  token: {{NUTRITIONIST_ACCESS_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
