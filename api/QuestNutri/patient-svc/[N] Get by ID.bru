meta {
  name: [N] Get by ID
  type: http
  seq: 6
}

get {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/patient/:patientId?includeDiets=true&includeWeights=true
  body: none
  auth: bearer
}

params:query {
  includeDiets: true
  includeWeights: true
}

params:path {
  patientId: {{PATIENT_ID}}
}

auth:bearer {
  token: {{NUTRITIONIST_ACCESS_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
