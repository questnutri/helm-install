meta {
  name: [N] [P] Get all
  type: http
  seq: 2
}

get {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/record/weight/patient/:patientId/all
  body: none
  auth: bearer
}

params:query {
  ~limit: 1
}

params:path {
  patientId: {{PATIENT_ID}}
}

auth:bearer {
  token: {{NUTRITIONIST_ACCESS_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
