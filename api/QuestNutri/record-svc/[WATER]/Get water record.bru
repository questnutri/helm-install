meta {
  name: Get water record
  type: http
  seq: 2
}

get {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/record/water/:waterGoalId
  body: none
  auth: inherit
}

params:path {
  waterGoalId: {{WATER_GOAL_ID}}
}

settings {
  encodeUrl: true
  timeout: 0
}
