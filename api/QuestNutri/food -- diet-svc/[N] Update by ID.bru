meta {
  name: [N] Update by ID
  type: http
  seq: 4
}

patch {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/diet/foods/:foodId
  body: json
  auth: bearer
}

params:path {
  foodId: {{createdFood}}
}

auth:bearer {
  token: {{NUTRITIONIST_ACCESS_TOKEN}}
}

body:json {
  {
    
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
