meta {
  name: Change password
  type: http
  seq: 4
}

post {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/auth/change-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{NUTRITIONIST_ACCESS_TOKEN}}
}

body:json {
  {
    "currentPassword": "123",
    "newPassword": "456"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
