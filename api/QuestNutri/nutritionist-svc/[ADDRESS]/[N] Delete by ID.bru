meta {
  name: [N] Delete by ID
  type: http
  seq: 6
}

delete {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/nutritionist/address/:addressId
  body: none
  auth: inherit
}

params:path {
  addressId: {{LAST_CREATED_ADDRESS_ID}}
}

settings {
  encodeUrl: true
  timeout: 0
}
