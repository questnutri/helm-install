meta {
  name: [N] Get by ID
  type: http
  seq: 4
}

get {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/nutritionist/address/:addressId
  body: none
  auth: inherit
}

params:path {
  addressId: {{LAST_CREATED_ADDRESS_ID}}
}

settings {
  encodeUrl: true
  timeout: 0
}
