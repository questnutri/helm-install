meta {
  name: [A] [N] [P] Ack
  type: http
  seq: 2
}

post {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/notification/ack
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

body:json {
  {
    "ids": []
  }
}

vars:pre-request {
  TOKEN: {{PATIENT_ACCESS_TOKEN}}
  ~TOKEN: {{ADMIN_ACCESS_TOKEN}}
  ~TOKEN: {{NUTRITIONIST_ACCESS_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
