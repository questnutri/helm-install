meta {
  name: [A] [N] [P] Ack
  type: http
  seq: 3
}

post {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/notification/ack
  body: json
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

body:json {
  {
    "ids": ["395ca38f-3df7-40b6-b57c-bb4056f24302"]
  }
}

vars:pre-request {
  TOKEN: {{PATIENT_ACCESS_TOKEN}}
  ~TOKEN: {{ADMIN_ACCESS_TOKEN}}
  ~TOKEN: {{NUTRITIONIST_ACCESS_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
