meta {
  name: [A] [N] [P] Me
  type: http
  seq: 1
}

get {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/notification/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{TOKEN}}
}

vars:pre-request {
  TOKEN: {{PATIENT_ACCESS_TOKEN}}
  ~TOKEN: {{ADMIN_ACCESS_TOKEN}}
  ~TOKEN: {{NUTRITIONIST_ACCESS_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
