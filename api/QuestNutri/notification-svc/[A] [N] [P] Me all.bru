meta {
  name: [A] [N] [P] Me all
  type: http
  seq: 2
}

get {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/notification/me/all
  body: none
  auth: bearer
}

auth:bearer {
  token: {{PATIENT_ACCESS_TOKEN}}
}

vars:pre-request {
  ~TOKEN: {{PATIENT_ACCESS_TOKEN}}
  ~TOKEN: {{ADMIN_ACCESS_TOKEN}}
  ~TOKEN: {{NUTRITIONIST_ACCESS_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
