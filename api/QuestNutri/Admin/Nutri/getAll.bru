meta {
  name: getAll
  type: http
  seq: 1
}

get {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/admin/nutritionist/all?approved=false
  body: none
  auth: bearer
}

params:query {
  approved: false
}

auth:bearer {
  token: {{ADMIN_ACCESS_TOKEN}}
}

settings {
  encodeUrl: true
}
