meta {
  name: [A] Login
  type: http
  seq: 1
}

post {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/admin/login
  body: json
  auth: none
}

body:json {
  {
    "email": "{{email}}",
    "password": "{{password}}"
  }
}

vars:pre-request {
  email: admin@questnutri.com
  password: 
}

vars:post-response {
  ADMIN_ACCESS_TOKEN: res("accessToken")
  ADMIN_REFRESH_TOKEN: res("refreshToken")
}

settings {
  encodeUrl: true
  timeout: 0
}
