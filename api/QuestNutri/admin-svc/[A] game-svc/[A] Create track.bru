meta {
  name: [A] Create track
  type: http
  seq: 3
}

post {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/game/tracks
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Weight registers",
    "fields": [
      {
        "name": "",
        "type": ""
      }
    ]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
