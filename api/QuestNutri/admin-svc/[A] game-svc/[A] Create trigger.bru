meta {
  name: [A] Create trigger
  type: http
  seq: 4
}

post {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/trigger
  body: json
  auth: bearer
}

body:json {
  {
    "trackId": "",
    "listenerId": "",
    "status": 201
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
