meta {
  name: [A] Create achievement
  type: http
  seq: 2
}

post {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/game/achievements
  body: json
  auth: inherit
}

body:json {
  {
    "name": "50 water records",
    "target": 50,
    "trackId": ""
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
