meta {
  name: [A] Me
  type: http
  seq: 5
}

get {
  url: {{HTTP_PROTOCOL}}://{{GATEWAY_URL}}/admin/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ADMIN_ACCESS_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
